{
  "name": "451-docs",
  "version": "2.0.0",
  "description": "Personal notes site — slug-based static site",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "check:js": "node scripts/check-js.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check": "npm run check:js && npm run check:links"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
