{
  "name": "451-docs",
  "version": "2.0.0",
  "description": "Personal notes site — slug-based static site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "netlify dev"
  },
  "engines": {
    "node": ">=18"
  }
}